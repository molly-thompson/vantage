[project]
name = "vantage"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "django>=6.1",
]

[dependency-groups]
dev = [
    "django-pytest>=0.2.0",
    "pytest>=9.1.1",
    "python-dotenv>=1.2.3",
    "ruff>=0.16.4",
    "ty>=0.0.74",
]
